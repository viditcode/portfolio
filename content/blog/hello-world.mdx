---
title: "Hello, this is my first post"
description: "A placeholder post explaining how this blog is structured — replace this once you've picked a niche."
date: "2026-08-16"
tags: ["meta"]
---

This is a placeholder post so the blog section isn't empty while you decide on a niche.

## How to add a real post

1. Create a new `.mdx` file in `content/blog/`, e.g. `content/blog/my-first-real-post.mdx`.
2. Add frontmatter at the top (the `---` block above) — `title`, `description`, `date`, and `tags`.
3. Write the body in normal Markdown below it. Headings, code blocks, lists all work.
4. Delete this file once you've published something real.

## A note on what makes a good first post

Given your background, a strong first real post could be something like *"What I learned building a Playwright framework for a real booking flow"* — specific, grounded in something you actually did, not generic advice. That's the kind of post that both ranks well over time and reads as credible to another engineer.
